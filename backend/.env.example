# Zuvomo Backend Environment Configuration

# Database Configuration
DB_HOST=localhost
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=zuvomo_db

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_here
JWT_REFRESH_SECRET=your_super_secret_refresh_key_here

# Server Configuration
PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:3002

# Email Configuration (for OTP)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASSWORD=your_app_password

# OAuth Configuration - Google
# To set up Google OAuth:
# 1. Go to https://console.developers.google.com/
# 2. Create a new project or select existing
# 3. Enable Google+ API and Google OAuth2 API
# 4. Create OAuth 2.0 credentials
# 5. Add authorized redirect URIs:
#    - http://localhost:5000/auth-enhanced/oauth/google/callback (development)
#    - https://yourdomain.com/api/auth-enhanced/oauth/google/callback (production)
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_REDIRECT_URI=http://localhost:5000/auth-enhanced/oauth/google/callback

# OAuth Configuration - LinkedIn
# To set up LinkedIn OAuth:
# 1. Go to https://www.linkedin.com/developers/
# 2. Create a new app
# 3. Add OAuth 2.0 scopes: openid, email, profile
# 4. Add authorized redirect URLs:
#    - http://localhost:5000/auth-enhanced/oauth/linkedin/callback (development)
#    - https://yourdomain.com/api/auth-enhanced/oauth/linkedin/callback (production)
LINKEDIN_CLIENT_ID=your_linkedin_client_id
LINKEDIN_CLIENT_SECRET=your_linkedin_client_secret
LINKEDIN_REDIRECT_URI=http://localhost:5000/auth-enhanced/oauth/linkedin/callback

# Optional: Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Optional: File Upload
MAX_FILE_SIZE=10485760
UPLOAD_PATH=./uploads